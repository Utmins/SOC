# 🧩 CrownJewel-1 — Guide
### Практическое руководство по расследованию NTDS dumping attack

---

## 1. Введение

Контроллер домена Forela подвергся атаке. Предполагается, что учётная запись администратора домена скомпрометирована, и есть подозрение, что злоумышленник разместил базу данных NTDS.dit на контроллере домена. Мы только что получили оповещение об использовании vssadmin на контроллере домена. Поскольку это не является частью обычного расписания, у нас есть веские основания полагать, что злоумышленник использовал эту утилиту LOLBIN, чтобы получить доступ к жемчужине доменной среды. 

Предоставленные артефакты:

- Дамп таблицы файловой системы:    `$MFT`
- Журнал событий Windows:           `Microsoft-Windows-NTFS.evtx`, `Security.evtx`, `Security.evtx`

Задача - провести анализ предоставленных артефактов для быстрой сортировки и, по возможности, как можно скорее отключите злоумышленника.

> ⚠️ Данное практическое задание демонстрирует способ обнаружения *дамп-атаки* на базу данных **NTDS.dit**, которая является одной из самых опасных атак на Active Directory (AD).

---

## 2. Цели анализа

1. **Выявить факт создания теневой копии и момент её использования**  
   *Ожидаемый результат:* UTC-временную метку запуска *vssadmin*/*vssvc*/*ntdsutil* и связать её с процессными событиями (4688/7045/7036) в `Security.evtx` / `System.evtx`.

2. **Определить субъект действия (Account Name / Machine Name)**  
   *Ожидаемый результат:* Наименование учётной записи (Account Name / LogonID / SID) или компьютер (hostname, MAchine Name а типа DC01$), который инициировал операцию создания снимка/копирования.

3. **Найти и подтвердить местоположение дампа NTDS.dit и сопутствующих hive-файлов**  
   *Ожидаемый результат:* Полный путь (C:... или пользовательские папки), размер и временную метку созданного файла(ов) (NTDS.dit + HKLM\...).

4. **ВИдентифицировать используемые инструменты и методы**  
   *Ожидаемый результат:* Использованные утилиты и методы, а также где именно оставлены артефакты их запуска.

5. **Сопоставить артефакты файловой системы и событий (MFT ↔ EVTX ↔ USN/LogFile)**  
   *Ожидаемый результат:* Связать запись(и) в $MFT с событиями создания/копирования и Volume Correlation ID для определения пути операции.

---

## 3. Основные события при анализе NTDS dumping attack

Active Directory хранит информацию о домене в файле **NTDS.dit**, который по умолчанию расположен в каталоге *%SystemRoot%\ntds\* на контроллере домена. Этот файл содержит важную информацию о домене, включая хэши паролей пользователей, что делает его весьма привлекательной целью для злоумышленников. Чтобы получить доступ к файлу **NTDS.dit**, злоумышленник **должен иметь права администратора** в данной среде. Если у злоумышленника есть доступ к контроллеру домена, он может извлечь файл **NTDS.dit** вместе с разделом реестра **HKEY_LOCAL_MACHINE\SYSTEM**, содержащим всю информацию, необходимую для расшифровки данных **NTDS.dit**.

Хотя Active Directory блокирует этот файл во время работы (запрещая любые операции копирования), злоумышленник может использовать службу теневого копирования томов `vssadmin` для копирования тома и извлечения файла **NTDS.dit** из снимка. Он также может создать копию с помощью диагностического инструмента `NTDSUTIL.exe`, входящего в состав Active Directory. Кроме того, если у злоумышленника есть набор действительных учётных данных, он может использовать такие инструменты, как `crackmapexec`, для удалённого копирования файла NTDS.dit и его анализа.

После того, как злоумышленник извлечёт файл **NTDS.dit** и ветку реестра **HKLM\SYSTEM**, он сможет проводить последующие атаки в автономном режиме, не требуя доступа к AD.

`NTDSUTIL` — это встроенная утилита для управления базой данных NTDS на серверах Windows, которую злоумышленник может использовать для получения доступа к этой базе данных. Однако это оставляет мало признаков атаки, которые можно использовать для установления того, что дамп **NTDS.dit** был создан злоумышленником. Поэтому, мы будем использовать журналы приложений (`Security.evtx`, `Security.evtx`) и системы контроллера домена (`Microsoft-Windows-NTFS.evtx`).

Ключевые события Windows, на которые следует обратить внимание:
- **Event ID 4768** — Kerberos TGT Request — запрос Ticket Granting Ticket (AS-REQ).  
- **Event ID 4769** — Kerberos Service Ticket Request — запрос TGS (для SPN).  
- **Event ID 4732** — A member was added to a security-enabled local group — добавление пользователя в локальную группу.  
- **Event ID 4742** — A computer account was changed — изменение учетной записи компьютера.  
- **Event ID 5379** — Credential Manager credentials were read — чтение сохранённых учётных данных Credential Manager.  
- **Event ID 4696** — A primary token was assigned to a process (редкое, устаревшее, встречается при запуске задания/службы).
- **Event ID 5140** — A network share was accessed — доступ к сетевому ресурсу (share).  
- **Event ID 5142** — A network share was added — создание общего ресурса.  
- **Event ID 142** — Host Application startup — запуск PowerShell-хоста.  
- **Event ID 4688** — A new process has been created — создание нового процесса (это основной процесс creation в Windows).  
- **Event ID 7036** — Service state changed — служба изменила состояние (Started/Stopped).  
- **Event ID 4662** — An operation was performed on an object — часто наблюдается при LDAP / AD перечислениях.
- **Event ID 4698** — A scheduled task was created — создание задания планировщика.  
- **Event ID 4700** — A scheduled task was enabled.  
- **Event ID 4701** — A scheduled task was disabled.  
- **Event ID 4702** — A scheduled task was updated.  
- **Event ID 7040** — Service startup type changed — изменение типа запуска службы.  
- **Event ID 4799** — A security-enabled local group membership was enumerated — перечисление состава группы (часто признак recon).

---

## 4. Инструменты и ресурсы

| Категория | Назначение | Рекомендации |
|---|---|---|
| EVTX-парсинг и экспорт | Конвертация EVTX в CSV/JSON для быстрой фильтрации и поиска событий | `EvtxCmd.exe`, `MFTECmd.exe` |
| Журналы Windows | Фильтрации и поиска событий | `Event Viewer` |
| Timeline / визуализация | Корреляция событий по времени | `TimelineExplorer` |

> ⚠️ Все действия выполняйте только в изолированной лабораторной среде (VM / песочница). Работайте с копиями артефактов — не анализируйте подозрительные бинарники на продуктивных системах.

---

## 5. Порядок выполнения

Перед тем, как мы приступим к анализу `*.evtx` и `$MFT` файлов, рекомендуется привести их в единому виду. И если файлы `*.evtx` еще можно просмотреть через `Event Viewer`, то MFT-дамп просто так не просмотришь (его сперва нужно распарсить). Да и просматриват каждый `*.evtx` файл поотдельности и затем вручную сопоставлять временные метки - так себе удовольствие. Поэтому, мы воспользуемся двумя утилитами из набора **Эрика Циммермана** для преобразования (и при необходимости объединения результатов в один файл) имеющихся артефактов в csv-читаемый формат:

   -  **Преобразование и объединение `.evtx` файлов**
   ```Powershell
      evtxECmd.exe -d 'C:\...\...\...\CrownJewel-1\' --csv 'C:\...\...\...\CrownJewel-1\' --csvf NTFS-SEC-SYS.csv
   ```

   -  **Преобразование `$MFT` файла**
   ```Powershell
      MfTECmd.exe -f 'C:\...\...\...\CrownJewel-1\$MFT' --csv 'C:\...\...\...\CrownJewel-1\' --csvf MFT.csv
   ```

> ⚠️ Если Вам удобнее, то Вы можете преобразовать файлы в `*.json` формат и потом исследовать их либо в ручную, либо через различные SIEM-утилиты.

> ⚠️ Для просмотра `*.csv` файлов вы также можете использовать любой, удобный для Вас инструмент. Лично я предпочитаю **TimelineExplorer**. Поэтому в последующих заданиях я буду основываться на выводе, полученным при его использование.

Следующий шаг, который также всегда желательно делать, перед тем как приступить к основным моментам - проверить какие вообще события (*Event Code/ID* и/или *Map Descriptor*) фигугрируют в наших лог-файлах. Если у Вас достаточно знаний по идентификации события, основываясь только лишь на его номере, то Вы можете смотреть историю событий используя лишь *Event Code/ID*. Я же предпочитаю сверять номер *Event Code/ID* с его описанием *Map Descriptor*. Но к сожалению не все *Event Code/ID* имеют опиание по колонке *Map Descriptor*, поэтому наимоблее интересные события приходиться гуглить.

### Задание 0 — Обнаружение взлома учетной записи.
**Цель:** Подтвердить или опровергнуть факт взлома учетной записи (или повышение привелегий).

Хотя данное задание отсутствует в списке задачь, но так как нам было сказано **было оповещение об использовании `vssadmin` на контроллере домена**, а использование данной утилиты без наличия прав уровня *администратор* не представляется возможным. Поэтому нам сперва надо завикфировать факт либо взлома учетной записи *администратора*, либо повышения уровня доступа одного из пользователей (к примеру включения его в группу *администраторы*). Для это надо отфильтровать все события, которые могут иметь отношение к:

   -  запросам билетов *Kerberos* (4768/4769)
   -  добавления пользователй в административные группы (4732)
   -  изменение атрибутов учетной записи компьютера (4742)
   -  чтение данных учетной записи административного пользователя (5379)
   -  исследование списка участников группы (4799)
   -  запуск процесса от имени другого пользователя (4696)
   -  и другие аналогичные

### Задание 1 — Подтвердить факт использования утилиты для создания копий образов системы/дисков.
**Цель:** Обнаружить время запуска утилиты `vssadmin`.

Так как в предыдущем задании мы обнаружили факт использование учетной записи с административными правами доступа, то теперь остается выяснить когда и кем была запущена данная утилита

**Действия:**

1. Для определени факта и времени запуска данной утилиты (`vssadmin`) нам понадобиться журналы **SYSTEM** и **SECURITY**, но так как все наши журналы объеденены в один отпарсенный файл, то будем искать в нем, а за временной меткой можем обратиться непосредстенно в сам журнал `SYSTEM.evtx` или `ECURITY.evtx`. Более того, так как мы не можем быть на 100% уверены, что данная утилита была запущена пользователм вручную или как запланированное событие, то нам придется расширить список исследуемых событий:

   -  A network share object was accessed (5140)
   -  A network share object was added (5142)
   -  NTFS-formatted drive attached (142)
   -  A new process has been created (4688)
   -  Service started or stopped (7036)
   -  Operation performed on an object (4662)
   -  Scheduled Task created (4698)
   -  A scheduled task was enabled (4700)
   -  A scheduled task was disabled (4701)
   -  A scheduled task was updated (4702)
   -  Start type of a service has changed (7040)
  
2. Затем, среди отфильтрованных значений нам надо найти событие, которое содержит в своем описании одно из следующих ключевых слов:

   -  shadow
   -  copy
   -  volume
   -  vssadmin
   -  vss

Ответ:

```
2024-05-14 03:42:16
```

---

### Задание 2 — Определить учетную запись компьютера и принадлежность к группам, который запустил процесс.
**Цель:** Выяснить имел ли пользователь/копьютер, запустивший процесс копировани NTDS-дампа, необходимые привелегии и/или права доступа.

> ⚠️ Сперва следует объяснить один момент - сразу после запуска, `VSS` выполняет проверку безопасности, т.е. она валидирует, что пользователь, запустивший данную службу имеет нужные права (и, следовательно, принадлежности к группам, имеющим нужные права). Поэтому, сразу после запуска `VSS` появляются события, которые классифицируются как **A security-enabled local group membership was enumerated (4799)**

**Действия:**

Существует два способо, которые помогут определить группы, которые имели право запускать процесс копировани NTDS-дампа:

1. С большим количеством шагов, но с меньшим содержанием временного мусора:

   - Ранее мы определили временную метку запуска утилиты `VSS`, поэтому нас будут интересовать события идущеи после нее. Таким образом убираем все события до временной метки **2024-05-14 03:42:16**.

   - Затем в столбце **Event ID** выбираем событие с номером *4799* 

2. С меньшим количеством шагов, но может иметь лишний временной шум:

   - Как мы уже знаем, служба `vssvc.exe` используется для копирования NTDS-дампов. Поэтому, мы сразу можем отфильтровать все наши события, использую ключевое слово - `vssvc.exe`

   - Затем включить фильтр по **Event ID (4799)** и получим только те события, которые были вызваны службой `vssvc.exe`. Однако, аналогичный события могли быть вызване легетимным пользователем ранее. Поэтому, после получения результатов нам надо будет сравнить временные метки

Для определения имени учетной записи компьютера, запустившего службу `vssvc.exe`, также существуют два способа:

1. Если знаете как редактировать XML-запрос в **Event Viewer**

   - Если Вы используете **TimelineExplorer**, то Вам надо будет прокрутить результат вправо (до самого последнего столбца *Payload*) и в деталях одной из записей найти **CallerProcessId** и выписать его значение.

   - Переходите в **Event Viewer** и при редактирование XML-запроса, указываете данное значение вместе с номером **Event ID**

2. Ручной поиск в **Event Viewer**

   - Тут все гораздо проще, но и долльше в тоже време, так как Вам придется среди всех **Event ID** в нужном журнале искать те события, которые сопвпадают по временной метке.

Ответы:

```
Administrators, Backup Operators, DC01$
```

### Задание 3 — Определить идентификатор процесса (в десятичной системе счисления) для процесса службы теневого копирования тома.
**Цель:** Высчитать значение **PID** в десятичной системе счисления для возможных последующих исследований.

В рамках этого сценария нам вполне достаточно и шестнадцатеричного вида значения **PID**. К тому же, большинство системных событий (`System.evtx`, `Security.evtx`) указывают **PID** в шестнадцатеричном виде. Однако, в других источниках значение **PID** может быть записано в другом формате (к примеру в десятичном виде). Среди таких источников могут быть - дампы памяти, диспетчер задач. Поэтому, для отчета или более глубоко анализа, всегда рекомендуется переводить значения в других форматы.

**Действия:**

Раз уж, в прошлом задании, мы выяснили занчение **PID**, то нам останется лишь только трансформировать его в нужный формат. Для этих целей лучше всего подходит он-лайн ресурс - https://cyberchef.org/. Однако надо быть аккуратным с выборот рецептов (*Recipe*), так как банальное из HEX в DEC выдаст вам неверный результат. Так, что лучше перепроверять вручную.

Но, если Вы все-таки решите использовать https://cyberchef.org/, то вот какjq рецепт (*Recipe*), надо использовать:

   -  **From Base** (если из шестнадцатеричного вида, то устанавливаете значение = 16)

Вот ответ:

```
4496
```

---

### Задание 4 — Найти значение Volume ID/GUID для дампа теневой копии при его монтировании.
**Цель:** Определить уникальное значение связуещего идентификатора событий, связанных с копированием NTDS-дампа.

Это задание со звездочкой, так как обнаруженные значение **Volume ID/GUID** не будут явяляться искомым результатом. Дело в том, что когда создаётся теневой снимок (shadow copy), в системных журналах записывается несколько событий:

   -  Служба VSS запущена.
   -  Начато создание снимка.
   -  Снимок успешно создан.
   -  Записана информация о месте хранения.

Все эти события относятся к одной операции, но появляются с разными Event ID. Таким образом, чтобы Windows (или аналитик DFIR) мог связать их между собой, каждому из этих событий присваивается один и тот же *Volume Correlation ID*.

> ⚠️ Основной журнал для этого заданяй - `Microsoft-Windows-NTFS.evtx`, но чтобы найти правильный *Volume Correlation ID*, нам нужно знать правильную временную метку.

**Действия:**

Как всегда у нас есть два способа определения нужного нам *Volume Correlation ID*:

1. Через использование **TimelineExplorer** 

   - Сперва отфильтровываем все события по **Event ID** = 4,9,10,300,303.

   - Затем в глобальном фильтре указываете *shadow* и выбираете только те события, которое удовлетворяют нашему временному интервалу.

   - Прокручиваете до последнего столбца *Paylod* и в его значениях ищите соответсвующие данные.

2. Через **Event Viewer** с конкретным журналом

   - Выбираем нужным нам журнал (в нашем случе это `Microsoft-Windows-NTFS.evtx`) и отфильтровываем записи, спользую те же самые **Event ID** = 4,9,10,300,303.

   - Прокручиваем до нужной нам временной метки и начинаем просматривать все события, в значиниях поля *DeviceName* которых будет встречаться слово *shadow*.

   - У найденного события находим поле *Volume Correlation ID* и выписывает его значение.

Вот ответ:

```
{06c4a997-cca8-11ed-a90f-000c295644f9}
```

---

### Задание 5 и 6 — Определите полный путь к сохраненному дампу базе данных NTDS на диске, а также дату создания дампа.
**Цель:** Выяснить расположения NTDS-дампа перед тем как он был скачен и его временную метку.

Данная информация содержится исключительно на дампе таблицы файловой системы (он же `$MFT`)

**Действия:**

Главная трудность определения местонахождения дампа **NTDS.dit** заключается в том, что дамп мог быть переименован после его создания и перемещен в новое место (и так несколько раз). В этом случае переименованную версию можно отследить по размеру файла. Но если в дам вносились изменения, или он создавался в урезанном формате, или разбивался, то тогда одного `$MFT` будет не достаточно. Придется брать дампы `$UsnJrnl` и `$LogFile`.

К счастью в нашем случае мы не имеем таких проблем (ни переименования, ни изменения размера, ени дробления). Поэтому поиск будет сводиться всего лишь к паре действий:

   - Сперва отфильтруем весь лишний мусор, указав к столбце **File Name** наименование искомого дампа - **NTDS.dit**
   
   - Если записей будет больше чем две, то сперва находим оригинал, он как правило храниться `C:\Windows\NTDS\NTDS.dit`; затем копируем его размер и отфильтровываем лишние записи по столбцу **File Size** указав в нем размер оригинального **NTDS.dit**. В результате мы длжны получить местонахождения дампа **NTDS.dit**.

С определением временной метки создания дампа **NTDS.dit** у вас не должно возникнуть проблем.

Вот ответ:
```
Location:   C:\Users\Administrator\Documents\backup_sync_Dc\Ntds.dit

Timestamp:  2024-05-14 03:44:22
```

---

### Задание 7 — Определить наименование скачаного куста реестра и его размер.
**Цель:** Обнаружить дамп куста реестра, созданного одновременно с дампаом **NTDS**  и его размер.

Определение местонахождения дампа куста реестра может иметь точно такие же трудности, что и определение местонахождения дампа **NTDS.dit** - переименование, дробление, изменение размера. И в таком случаем Вам также понадобятся дампы `$UsnJrnl` и `$LogFile`. Но нам и тут везет - ничего не менялось в нашем сценарии.

**Действия:**

В простом варианте нам бы стоило проверь туже папку, в которой мы нашли дамп **NTDS.dit**, но мы немного усложним себе задачу и представим, что дамп куста реестра был сохранен в другое папку. Поэтому, нам придется искать его не опираясь на прошлые обнаружения.

   - Самый просто способ - это сперва отфильтровать ненужный временной мусор, так как дамп `$MFT` содержит очень много записей

   - Искать любое совпадение с названием возможных кустов реестра (а их всего 5-7) по столбцу **File Name**: [SYSTEM], [SOFTWARE], [SAM], [SECURITY], [DEFAULT], [NTUSER.DAT], [USRCLASS.DAT]

Вот ответ:
```
Hive:    SYSTEM

Size:    17563648
```




